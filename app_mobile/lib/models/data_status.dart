class DataStatus {
  String name;

  var waterT;
  var waterEC;
  var waterSalt;
  var upper;
  var lower;
  var devices1;
  var devices2;

  var id;

  DataStatus(this.name, this.waterT, this.waterEC, this.waterSalt, this.upper,
      this.lower, this.devices1, this.devices2, this.id);
}
